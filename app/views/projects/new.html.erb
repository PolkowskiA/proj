<h1>New Project</h1>

<%= form_for @project, html: { multipart: true } do |f| %>
  <%= f.text_field :name %>

  <%= f.file_field :image %>

  <%= f.submit %>
<% end %>

<%= link_to 'Back', projects_path %>
